# Secrets - Ne jamais committer ce fichier
# Copier en .env et remplir les valeurs

# Token du GitLab Runner (généré dans GitLab > Settings > CI/CD > Runners)
GITLAB_RUNNER_TOKEN=

# Identifiants SMTP
SMTP_USER=
SMTP_PASSWORD=
